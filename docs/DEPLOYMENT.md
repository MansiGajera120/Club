# Deployment Guide

> Populated in Phase 12. Will cover backend (Docker / PM2 / reverse proxy), admin panel
> (static hosting / CDN), and mobile app (Play Store / App Store) release processes.

## Planned targets

| App          | Target                                   |
| ------------ | ---------------------------------------- |
| `backend`    | Node host / container behind Nginx + TLS |
| `admin_panel`| Static build served via CDN / Nginx      |
| `mobile_app` | Google Play & Apple App Store            |

_Step-by-step instructions, CI/CD, and environment provisioning are added in Phase 12._
