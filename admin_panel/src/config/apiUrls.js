/**
 * Canonical backend API base URLs (must include the `/api/v1` prefix).
 * Used when `VITE_API_URL` is not set explicitly.
 */
export const API_BASE_URLS = {
  local: 'http://localhost:5000/api/v1',
  production: 'https://club-1r4i.onrender.com/api/v1',
};
