# Base URL of the backend REST API (must include the /api/v1 prefix).
# Optional — defaults are applied automatically:
#   npm run dev  → http://localhost:5000/api/v1
#   npm run build / Vercel → https://club-1r4i.onrender.com/api/v1
# VITE_API_URL=http://localhost:5000/api/v1

# Display name shown in the admin panel UI.
VITE_APP_NAME=Sports Club Admin
