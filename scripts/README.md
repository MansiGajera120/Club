# Scripts

Cross-cutting dev/ops helper scripts for the workspace.

Planned scripts (added in later phases):

- `seed.ts` — seed the database with an admin user and sample clubs/events.
- `backup.sh` — snapshot MongoDB and the `uploads/` directory.
- `deploy.sh` — build & deploy backend + admin panel.

Run scripts from the workspace root unless noted otherwise.
